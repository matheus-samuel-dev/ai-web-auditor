# Docker removes a stopped service from DNS. Do not retain that temporary failure
# after its healthcheck recovers; a user-triggered retry must resolve it again.
# This file supplements the JRE defaults; all other security properties remain.
networkaddress.cache.negative.ttl=0
